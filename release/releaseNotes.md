# Flick 0.0.4

- 将每个插件及其依赖安装到独立工作区，避免依赖平铺、版本冲突和跨插件污染。
- 支持旧版平铺插件的兼容读取、自动迁移，以及包含完整依赖闭包的离线包导入导出。
- 修复 Windows 10 资源管理器多选文件后，超级面板只能读取第一个文件的问题。
- 使用原生 Shell 视图快照选区，统一键盘快捷键和鼠标触发的多文件读取行为。
- 将文件状态读取改为并发异步超时处理，避免网络路径或大量文件导致超级面板卡死。
